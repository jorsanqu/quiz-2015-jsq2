<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8"><title>Quiz</title>
		<link rel='stylesheet' href='/stylesheets/style.css' />
	</head>
	<body>
		<table>
			<% var i,aux=""; for (i=0; i < quizes.length; i++) { 
                                  aux = "/quizes/" + quizes[i].id         %>
                                  <tr><td><a href="<%= aux %>"><%= quizes[i].pregunta %></a></td></tr>

			<% } %>
		</table>
	</body>
</html>
